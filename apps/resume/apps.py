from django.apps import AppConfig


class ResumeConfig(AppConfig):
    """Configuration for the resume application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.resume"